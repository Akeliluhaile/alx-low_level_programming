#include "main.h"
/**
 *_abs - gives abslote value of an intiger
 * @n:intiger
 * 
 */
int _abs(int n)
{


	if (n > 0)
	{
		_putchar(n);
	}
	else if (n < 0)
	{
		abs = -1 * n;
		_putchar(abs);
	}
	else
	{
		_putchar(0);
	}
	
}
