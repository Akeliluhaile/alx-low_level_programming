#include "main.h"
/**
 *_abs - gives abslote value of an intiger
 * @int:intiger
 * Return: Always (success)
 */
int _abs(int)
{


	if (int > 0)
	{
		_putchar(int);
	}
	else if (int < 0)
	{
		_putchar(-1 * int);
	}
	else
	{
		_putchar(0);
	}
	return (0);
}
