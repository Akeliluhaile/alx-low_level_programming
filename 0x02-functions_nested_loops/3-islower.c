#include "main.h"
/**
 * _islower - identify low case
 * Return: 1 if small else 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
