#include <stdio.h>
/**
 * main -  prints some letter in alphabet
 * Return: Always (success)
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
		if (a != 'e' && a != q)
		{
			putchar(a);
		}
	}
	putchar('\n');

	return (0);
}

